'use strict';
const test=require('node:test'); const assert=require('node:assert/strict'); const fs=require('node:fs'); const path=require('node:path');
const root=path.resolve(__dirname,'../../..');
test('G4 feature flag defaults OFF and allocation batch remains independently controlled',()=>{const f=require(path.join(root,'src/config/featureFlags')); const a=process.env.PERF_CLOSEOUT_AR_WRITE_BULK_V1,b=process.env.PERF_CLOSEOUT_ALLOCATION_POSTEDREFS_BATCH_V1; delete process.env.PERF_CLOSEOUT_AR_WRITE_BULK_V1; delete process.env.PERF_CLOSEOUT_ALLOCATION_POSTEDREFS_BATCH_V1; try{assert.equal(f.FLAGS.closeoutArWriteBulkV1(),false);assert.equal(f.FLAGS.closeoutAllocationPostedRefsBatchV1(),false);process.env.PERF_CLOSEOUT_AR_WRITE_BULK_V1='1';assert.equal(f.FLAGS.closeoutArWriteBulkV1(),true);assert.equal(f.FLAGS.closeoutAllocationPostedRefsBatchV1(),false);process.env.PERF_CLOSEOUT_ALLOCATION_POSTEDREFS_BATCH_V1='1';assert.equal(f.FLAGS.closeoutArWriteBulkV1(),true);assert.equal(f.FLAGS.closeoutAllocationPostedRefsBatchV1(),true);process.env.PERF_CLOSEOUT_AR_WRITE_BULK_V1='0';assert.equal(f.FLAGS.closeoutArWriteBulkV1(),false);assert.equal(f.FLAGS.closeoutAllocationPostedRefsBatchV1(),true);}finally{a===undefined?delete process.env.PERF_CLOSEOUT_AR_WRITE_BULK_V1:process.env.PERF_CLOSEOUT_AR_WRITE_BULK_V1=a;b===undefined?delete process.env.PERF_CLOSEOUT_ALLOCATION_POSTEDREFS_BATCH_V1:process.env.PERF_CLOSEOUT_ALLOCATION_POSTEDREFS_BATCH_V1=b;}});
test('G4 AR-DEBT-ADJUSTMENT remains outside allocation-generated batch categories',()=>{const s=require(path.join(root,'src/services/accounting/closeout/CloseoutArBatchPostingService'));assert.equal(s.ELIGIBLE_CATEGORIES.has('AR-DEBT-ADJUSTMENT'),false);});
test('G4 reward/offset invariant remains one AR-REWARD-ALLOWANCE builder branch',()=>{const source=fs.readFileSync(path.join(root,'src/services/accounting/OrderPaymentAllocationService.js'),'utf8');const matches=source.match(/push\('AR-REWARD-ALLOWANCE'/g)||[];assert.equal(matches.length,1);assert.doesNotMatch(source,/push\('AR-(?:OFFSET|REWARD-OFFSET)'/);});
