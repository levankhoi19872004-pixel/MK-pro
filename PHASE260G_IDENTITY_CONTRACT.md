# Phase260G Identity Contract

Identity now includes semanticRole, financialComponent, normalized business source identity, order identity, componentSourceIdentity and sourceVersion.

Format: <semanticRole>|<financialComponent>|<sourceType>:<sourceId>|order:<orderId>|component:<componentSourceIdentity>|v:<sourceVersion>.

Amount, createdAt and ledger id are not part of business identity.
